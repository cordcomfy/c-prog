// write a c program to calculate the distance travelled by an object

#include <stdio.h>
int main () {
 int v = 4, t = 1, distance;
 distance = v * t;
 printf("the distance travelled by an object is given by %d\n", distance);
return 0;
}
